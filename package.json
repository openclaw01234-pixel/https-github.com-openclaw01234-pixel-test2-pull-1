{
  "name": "mxplayer-pro",
  "version": "1.1.0",
  "description": "MX Player Pro - Next-Gen Video Platform",
  "private": true,
  "scripts": {
    "build:web": "node scripts-build/copy-web.js",
    "build:android": "npx cap sync android && cd android && ./gradlew assembleDebug",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.4",
    "@capacitor/cli": "^8.3.4",
    "@capacitor/core": "^8.3.4"
  }
}
